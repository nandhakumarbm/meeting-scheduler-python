# meeting-scheduler-python
🗓️ A simple Python-based meeting scheduler that prevents overlapping meetings for each user. Supports adding, viewing, and checking meeting overlaps with clean console interaction.
